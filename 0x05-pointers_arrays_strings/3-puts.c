#include "main.h"

/**
 * _puts - prints a string, followed by a new line, to stdout.
 * @str: string to be printed
 * Return: return always nothing (success)
 */
void _puts(char *str)
{
	while (*str)
	{
		_putchar(char);
		str++;
	}
	_putchar('\n');
}
